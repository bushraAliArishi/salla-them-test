{% extends 'layouts/default.twig' %}

{% block title %}{{ app.title }} - {{ lang('home') }}{% endblock %}

{% block content %}
    {# Dynamic components from Twilight.json will be rendered here #}
    {% for component in components.home %}
        {% include 'components/' ~ component.id ~ '.twig' %}
    {% endfor %}
{% endblock %}
