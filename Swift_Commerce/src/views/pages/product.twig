{% extends 'layouts/default.twig' %}

{% block title %}{{ product.name }} - {{ app.title }}{% endblock %}

{% block content %}
    <section class="product-page-swift">
        <div class="container">
            <div class="product-main-area">
                <div class="product-gallery">
                    {# Product gallery component #}
                </div>
                <div class="product-info-area">
                    {% include 'components/product_info_compact.twig' %}
                </div>
            </div>
            {% include 'components/customer_reviews.twig' %}
        </div>
    </section>
{% endblock %}
