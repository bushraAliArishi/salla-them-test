<div class="product-info-compact">
    <h1>{{ product.name }}</h1>
    <p class="price">{{ product.price | currency }}</p>
    <div class="short-description">
        {{ product.short_description }}
    </div>
    <div class="product-options">
        {# Options/Variants selection logic goes here #}
    </div>
    <button class="btn-primary">{{ lang('add_to_cart') }}</button>
    <button class="btn-secondary">{{ lang('buy_now') }}</button>
</div>
