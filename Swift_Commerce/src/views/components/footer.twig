<footer>
    <div class="container">
        <div class="footer-columns">
            <div class="col">
                <h4>{{ lang('swift_commerce') }}</h4>
                <p>Built for speed and conversions. Get your products to your customers faster.</p>
            </div>
            <div class="col">
                <h4>{{ lang('customer_service') }}</h4>
                <ul>
                    <li><a href="{{ url('/contact') }}">{{ lang('contact_us') }}</a></li>
                    <li><a href="{{ url('/shipping') }}">{{ lang('shipping_info') }}</a></li>
                    <li><a href="{{ url('/returns') }}">{{ lang('returns') }}</a></li>
                </ul>
            </div>
            <div class="col">
                <h4>{{ lang('stay_connected') }}</h4>
                {# Social media icons would go here #}
            </div>
        </div>
        <div class="copyright">
            &copy; {{ "now"|date("Y") }} {{ app.title }}. {{ lang('all_rights_reserved') }}.
        </div>
    </div>
</footer>
