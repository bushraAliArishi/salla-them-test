<section class="promo-banner">
    <div class="container">
        <p>{{ lang('promo_message') }}</p>
        <a href="{{ url('/sale') }}" class="btn-primary">{{ lang('shop_sale') }}</a>
    </div>
</section>
