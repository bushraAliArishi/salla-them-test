<section class="quick-links-grid">
    <div class="container">
        <div class="grid-3">
            <a href="{{ url('/new-arrivals') }}" class="quick-link-card">
                <h4>{{ lang('new_arrivals') }}</h4>
            </a>
            <a href="{{ url('/best-sellers') }}" class="quick-link-card">
                <h4>{{ lang('best_sellers') }}</h4>
            </a>
            <a href="{{ url('/sale') }}" class="quick-link-card">
                <h4>{{ lang('sale') }}</h4>
            </a>
        </div>
    </div>
</section>
